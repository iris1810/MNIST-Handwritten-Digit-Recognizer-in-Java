# MNIST-Handwritten-Digit-Recognizer-in-Java
This program implements a feed-forward neural network for classifying handwritten digits (0–9) from the MNIST dataset. It uses stochastic gradient descent (SGD) and backpropagation to learn from training examples.
